
# Reproducibility

Offline acceptance runs the R2B deterministic tests, sanitized R2C shadow, R2D deterministic validator, metric recomputation, and public-safety scan.

The private live enterprise snapshot and paid provider reruns are not bundled. Cryptographic identifiers and sanitized result summaries preserve the evidence boundary.
